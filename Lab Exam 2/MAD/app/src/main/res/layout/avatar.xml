<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <!-- Avatar Model -->
    <ImageView
        android:id="@+id/avatarImage"
        android:layout_width="match_parent"
        android:layout_height="500dp"
        android:scaleType="fitCenter"
        android:src="@drawable/avatar_model" />

    <!-- Color Swatches -->
    <LinearLayout
        android:layout_alignParentEnd="true"
        android:layout_margin="8dp"
        android:orientation="vertical"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <View android:layout_width="24dp" android:layout_height="24dp" android:background="#E1E1E1" />
        <View android:layout_width="24dp" android:layout_height="24dp" android:background="#333333" />
        <View android:layout_width="24dp" android:layout_height="24dp" android:background="#673AB7" />
    </LinearLayout>

    <!-- Outfit Options -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#F5F5F5">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="8dp">

            <!-- Outfit cards -->
            <LinearLayout
                android:layout_width="160dp"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="8dp">

                <ImageView android:layout_width="match_parent" android:layout_height="140dp" android:src="@drawable/outfit1" />
                <TextView android:text="My Outfit 1" />
                <TextView android:text="30 $" />
            </LinearLayout>

            <!-- Repeat for more outfits -->
        </LinearLayout>
    </HorizontalScrollView>
</RelativeLayout>
